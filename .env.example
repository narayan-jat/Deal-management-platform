# Copy to `.env` in the repo root (never commit `.env`).
# Values are examples only — use your own Supabase project.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_EMAIL_REDIRECT_TO=http://localhost:5173/dashboard
# Optional: used for shareable invite links in some flows
# VITE_APP_URL=http://localhost:5173
